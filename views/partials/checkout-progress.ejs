<div class="mb-8">
    <div class="flex justify-between items-center">
        <div class="flex flex-col items-center">
            <div class="w-10 h-10 flex items-center justify-center rounded-full <%= step >= 1 ? 'bg-green-600 text-white' : 'bg-gray-200 text-gray-600' %>">
                1
            </div>
            <span class="text-sm mt-1 <%= step >= 1 ? 'text-green-600 font-medium' : 'text-gray-500' %>">Address</span>
        </div>
        
        <div class="h-1 flex-grow mx-2 <%= step >= 2 ? 'bg-green-600' : 'bg-gray-200' %>"></div>
        
        <div class="flex flex-col items-center">
            <div class="w-10 h-10 flex items-center justify-center rounded-full <%= step >= 2 ? 'bg-green-600 text-white' : 'bg-gray-200 text-gray-600' %>">
                2
            </div>
            <span class="text-sm mt-1 <%= step >= 2 ? 'text-green-600 font-medium' : 'text-gray-500' %>">Payment</span>
        </div>
        
        <div class="h-1 flex-grow mx-2 <%= step >= 3 ? 'bg-green-600' : 'bg-gray-200' %>"></div>
        
        <div class="flex flex-col items-center">
            <div class="w-10 h-10 flex items-center justify-center rounded-full <%= step >= 3 ? 'bg-green-600 text-white' : 'bg-gray-200 text-gray-600' %>">
                3
            </div>
            <span class="text-sm mt-1 <%= step >= 3 ? 'text-green-600 font-medium' : 'text-gray-500' %>">Review</span>
        </div>
        
        <div class="h-1 flex-grow mx-2 <%= step >= 4 ? 'bg-green-600' : 'bg-gray-200' %>"></div>
        
        <div class="flex flex-col items-center">
            <div class="w-10 h-10 flex items-center justify-center rounded-full <%= step >= 4 ? 'bg-green-600 text-white' : 'bg-gray-200 text-gray-600' %>">
                4
            </div>
            <span class="text-sm mt-1 <%= step >= 4 ? 'text-green-600 font-medium' : 'text-gray-500' %>">Confirm</span>
        </div>
    </div>
</div>